# Banking — Bun configuration
# Next.js fintech app with Drizzle ORM + Plaid/Dwolla

[build]
# Next.js build via Bun runtime
loader = false

[test]
coverage = true
root = "./src"

[install]
optional = true
dev = true
peer = true
