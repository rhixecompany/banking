# See AGENTS.md for the canonical repository rules and plan thresholds.

This file intentionally points to AGENTS.md as the single source of truth for human-readable rules and enforcement guidance. Use `.opencode/verify-rules.config.json` and `scripts/verify-rules.ts` for machine-enforced checks and allowlists.

Plan file location: `.opencode/commands/` (see AGENTS.md for details)

Last Reviewed: 2026-04-17
