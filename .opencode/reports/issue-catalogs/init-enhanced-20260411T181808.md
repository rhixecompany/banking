# Init-Enhanced — Issue Catalog (2026-04-11T18:18:08)

Summary

- Branch: feature/init-enhanced-2026-04-11-e7a4bd3f
- Output capture: .opencode/outputs/init-enhanced/20260411T181808/
- Goal: Parse Phase 2 validation outputs and record issues for triage and fix

Validation Snapshot

- format: check — passed (format-check.txt)
- type-check — passed after a targeted test fix (type-check.txt)
- lint:strict — no warnings captured (lint-strict.txt)
- unit tests (vitest) — passed (test-browser.txt)
- E2E tests (Playwright) — partial: 3 failing tests, server-side errors/logs present (test-ui.txt)

High Priority Issues (action recommended)

1. Playwright E2E Failures — My Wallets suite
   - Files / locations:
     - tests/e2e/my-wallets.spec.ts (failing tests at lines referenced in Playwright run)
   - Symptoms / evidence:
     - 3 tests failed in the My Wallets suite:
       - "should show page title and description" — test id in output: x 24
       - "should show total balance card" — test id in output: x 25
       - "should list seeded wallet accounts" — test id in output: x 26
     - Playwright output excerpt: .opencode/outputs/init-enhanced/20260411T181808/test-ui.txt (see lines ~640-710 for failures)
     - Failure artifacts (screenshots) referenced in Playwright output:
       - test-results\my-wallets-My-Wallets-Page-007f0--page-title-and-description-chromium\test-failed-1.png
       - test-results\my-wallets-My-Wallets-Page-ee467-uld-show-total-balance-card-chromium\test-failed-1.png
       - test-results\my-wallets-My-Wallets-Page-b5a9d-list-seeded-wallet-accounts-chromium\test-failed-1.png
   - Likely causes / notes:
     - Tests time out / page not rendering expected elements; server logs show intermittent `Error: aborted` with code ECONNRESET while serving dashboard/my-wallets routes.
     - Browser console shows: "The Plaid link-initialize.js script was embedded more than once." Duplicate script embedding can cause Plaid Link to misbehave and break page rendering or client-side initialization that tests expect.
   - Suggested fixes / triage steps:
     1. Reproduce failing tests locally in isolation: run `npx playwright test tests/e2e/my-wallets.spec.ts -g "My Wallets Page" --project=chromium` after ensuring port 3000 is free.
     2. Open the failing screenshots to inspect the page state (selectors missing, error overlays, etc.).
     3. Search the codebase for duplicate Plaid script injection points (components that add `https://cdn.plaid.com/link/v2/stable/link-initialize.js` or call Plaid embed multiple times). Candidates: components that wrap Plaid Link, pages (my-wallets), app-level layout, Plaid wrapper utilities, and any useEffect that appends a script tag.
     4. If Plaid is embedded in both a layout and a component, change to a single embed (prefer the page-level or a single shared client component) or guard against multiple inserts (check if window.Plaid is present before injecting).
     5. For ECONNRESET / aborted errors:
        - Inspect server logs around the stack traces (test-ui.txt lines ~270-640 show aborts). Determine whether aborts come from dev server crashes, network resets, or test runner canceling navigation.
        - Try increasing Playwright timeouts for the failing tests (temporary) to see if flakiness resolves; prefer fixing root cause if dev server sometimes aborts requests.
     6. Run the isolated tests repeatedly to confirm flakiness vs deterministic failure.

2. Browser Console Warning — Plaid duplicate embed (client-side)
   - Files / locations: observed in Playwright browser console during /dashboard and /my-wallets page loads (test-ui.txt lines ~314 and ~683)
   - Symptoms / evidence: console warning: "The Plaid link-initialize.js script was embedded more than once. ..."
   - Suggested fix: Same as item 1 step 3-4. Add a small defensive guard where the script is injected.

3. Server-side uncaughtException: Error: aborted (ECONNRESET)
   - Files / locations: logged by dev server in test-ui capture (test-ui.txt lines ~271-276 and ~629-639)
   - Symptoms / evidence: Several uncaughtException entries with code 'ECONNRESET' and message 'Error: aborted'
   - Likely causes:
     - Playwright terminating a request early (navigation aborted) or dev server closing a connection unexpectedly
     - Transient network resource failures during test-run load (static assets, third-party calls)
   - Suggested fixes / triage steps:
     1. Reproduce with a single failing test; observe server stack trace in full (run Playwright with DEBUG or attach console logs).
     2. Add try/catch around critical server-side network calls used during page render (actions that call Plaid, Dwolla, or external APIs) to prevent uncaught exceptions from bubbling and crashing the request handler.
     3. Ensure tests mock or sandbox third-party calls (Plaid/Dwolla) where appropriate to avoid intermittent network errors in E2E runs.

Low / Informational Issues

- Tests / Type Fix Done: A TypeScript unit-test issue was fixed in tests/unit/dal/dwolla-dal.test.ts (null -> undefined / empty string). This removed the tsc error and is already committed.
- Lint / format: passed. No action required.

Reproduction Commands

1. Install deps (if not installed): `npm ci`
2. Free port 3000 (Windows PowerShell): $pids = Get-NetTCPConnection -LocalPort 3000 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique
   if ($pids) { $pids | ForEach-Object { Stop-Process -Id $\_ -Force } }
3. Run isolated Playwright suite for My Wallets: cross-env PLAYWRIGHT_PREPARE_DB=true npx playwright test tests/e2e/my-wallets.spec.ts -g "My Wallets Page" --project=chromium -s

Artifacts

- Raw captures: .opencode/outputs/init-enhanced/20260411T181808/{format-check.txt,type-check.txt,lint-strict.txt,test-browser.txt,test-ui.txt}
- Playwright artifacts referenced in test-ui.txt: test-results/, playwright-report/

Next Steps (recommended order)

1. Confirm you want me to proceed with reproducing the three failing Playwright tests locally (I will free port 3000, run the tests in isolation, capture fresh logs and screenshots). (Recommended)
2. If reproduced, search for duplicate Plaid embed points and patch to prevent double-inserts. Create a small focused commit per fix. Run the isolated tests again.
3. If ECONNRESET persists, add defensive error handling around server-side external calls used during these pages, and/or mock those calls in Playwright setup.
4. Re-run full validation sequence and capture outputs to .opencode/outputs/init-enhanced/<new-timestamp>/ and summarize results.

If you want me to continue now, choose one:

- Proceed to reproduce the My Wallets Playwright tests in isolation and triage (I will run Playwright and gather logs).
- Stop here and wait for further instructions (I will not run tests or change code).

Generated: 2026-04-11T18:18:08
