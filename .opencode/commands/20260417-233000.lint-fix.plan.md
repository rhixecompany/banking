Title: Lint Fix Plan — 2026-04-17 23:30 UTC

Provenance: read ci-reports/20260417-232921/lint-fix-report.txt, ci-reports/20260417-232921/lint-strict-report.txt

Summary

- This plan implements grouped lint fixes derived from the CI lint reports stored in ci-reports/20260417-232921.
- Changes will be made on branch: lint/fix/by-rule (local only, no push).
- Verification: npm run format && npm run type-check && npm run lint:strict && npm run test:browser

Decisions (user-confirmed)

- Console handling: Add ESLint override for scripts/ to allow console
- Tailwind unknown classes: Whitelist design-system class names in ESLint plugin config (eslint.config.mts)
- null policy: Replace null with undefined where safe
- require-await: Remove unnecessary async keywords
- Hooks: Add createLinkToken to deps or memoize in parent
- TS parsing errors: Convert problematic .js files to .ts/.tsx or move into included folders
- Automated fixes: Run npm run format && npm run lint:fix first
- Commit strategy: Hybrid — group small rules, per-file commits for large changes
- Plan file: this file (because >5 files will be touched)

High-Level Steps

1. Create branch: git checkout -b lint/fix/by-rule
2. Run automated cleanup:
   - npm run format
   - npm run lint:fix
   - npm run type-check
   - npm run lint:strict
3. Apply config updates (eslint.config.mts):
   - Add scripts/ override to allow console
   - Whitelist tailwind class names used by project
4. Manual grouped edits (one commit per rule or per-file for large changes):
   - no-console (config + targeted edits)
   - unicorn/no-null (replace null -> undefined)
   - require-await (remove async)
   - eqeqeq (use ===)
   - no-param-reassign (avoid mutating params)
   - react-hooks/exhaustive-deps (add deps or memoize)
   - jsdoc param fixes
   - perfectionist sorting fixes
   - no-floating-promises
   - tests: testing-library/no-container, jsx-a11y
5. Convert/adjust JS files causing TypeScript project parsing errors (convert to .ts or include in tsconfig)
6. After each grouped commit run: npm run format && npm run type-check && npm run lint:strict
7. Final verification: npm run test:browser and produce final summary

Commit message format

- Use Conventional Commits. Each commit body will include a one-line provenance note.

Risks & Mitigations

- null -> undefined may reveal subtle differences; mitigated by running type-check and tests after changes.
- Adding hooks deps may change runtime calls; prefer memoizing functions in parents when feasible.
- Whitelisting tailwind classes must be precise to avoid masking typos.

Next Action (implementation)

- Run the automated steps and report results. Then apply grouped edits as described.
