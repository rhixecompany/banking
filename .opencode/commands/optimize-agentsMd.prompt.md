## Plan: Comprehensive AGENTS.md,`.cursor/rules/*.mdc`, `.cursorrules`,`.github/copilot-instructions.md`,`**/SKILL.md`,`.opencode/instructions/*.md`,`.github/instructions/*.md` for Agentic Coding in Banking Repo

## Phase 1

Take this steps to generate the AGENTS.md,`.cursor/rules/*.mdc`, `.cursorrules`,`.github/copilot-instructions.md`,`**/SKILL.md`,`.opencode/instructions/*.md`,`.github/instructions/*.md` files:

1. Read the session and chat history.
2. Read and understand AGENTS.md,`.cursor/rules/*.mdc`, `.cursorrules`,`.github/copilot-instructions.md`,`**/SKILL.md`,`.opencode/instructions/*.md`,`.github/instructions/*.md`.
3. Analyze the codebase and existing documentation and read `package.json`.
4. Ask all needed Questions.

## Phase 2

1. Create or Update if existing AGENTS.md,`.cursor/rules/*.mdc`, `.cursorrules`,`.github/copilot-instructions.md`,`**/SKILL.md`,`.opencode/instructions/*.md`,`.github/instructions/*.md` Make it comprehensive and thorough.
2. Update all existing agentic documentation and rules to ensure consistency and completeness across the repository.
3. Update README.md and other relevant documentation to reflect the latest agentic standards and practices and to provide clear guidance for new contributors and to ensure alignment with the AGENTS.md file and to maintain a cohesive development environment and workflow and verify compliance with the updated standards.
4. Review and refine the AGENTS.md,`.cursor/rules/*.mdc`, `.cursorrules`,`.github/copilot-instructions.md`,`**/SKILL.md`,`.opencode/instructions/*.md`,`.github/instructions/*.md` files to ensure clarity, accuracy, and usability for all contributors.
