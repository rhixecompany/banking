Read and Understand @docs/custom-components.md,@docs\test-context.md,@docs\app-pages.md files
As Reviewer persona using the read tool Read and Understand @docs/custom-components.md,@docs\test-context.md,@docs\app-pages.md files then Create an implementation plan to:
1: List all Next.js pages in ./app a Next.js Page should look like  this @page.tsx triage and group by route layouts.
2: Start with the First group route layouts which should be `(auth)` then `(admin)` then `(root)` and lastly `app\page.tsx` and locate all its custom components,dal,actions,tests.
3: For All its components,dal,actions,tests Modify them to be fully functional ensure to create and use any reusable dynamic generic custom components and saved to `./components/layouts` ensure to verify and valid all reusable dynamic generic custom components in `./components/layouts` use dry practices Update all test,helpers and configurations in `docs/test-context.md` use dry practices Harden all vitest and Playwright E2E specs by removing skipped/non-deterministic tests, standardizing assertions, and making authenticated scenarios deterministic with a seeded user.
4: Work through all Next.js pages sequentially and repeat the process.
5: Ensure to never run typecheck/lint/tests until all pages in the list are enhanced.
6: using the question tool ask all clarifying or needed questions then keep asking until you dont have further question then show me the plan with all new and old data .


modify @scripts\mcp-runner.ts to be a custom handler for installing mcp servers in Opencode/Copilot/Cursor. using the bash tool run `docker mcp gateway run --profile adminbot` to get a list of docker mcp servers the conferences it with @.opencode\mcp_servers.json if an mcp server is found at @.opencode\mcp_servers.json Catalog it then create all helpers for enhanced this file then update @opencode.json with all mcp servers in the Catalog using the @scripts\mcp-runner.ts file then delete all mcp servers in the Catalog from docker leave any mcp server not found ensure all mcp servers are load via @scripts\mcp-runner.ts
