# Init-Enhanced Issue Catalog — 2026-04-11T200000

This document records the actionable issues discovered during the Init-Enhanced discovery and triage effort and recommends minimal fixes or mitigations. Validation runs were intentionally skipped for this checkpoint — see the companion checkpoint file for what was committed.

## Summary

- Origin: Phase 1 discovery artifacts and local code inspection on branch `feature/init-enhanced-2026-04-11-e7a4bd3f`.
- Validation: Phase 2 validation commands were NOT run for this checkpoint (per instructions). The catalog below is derived from earlier discovery and code inspection.

## High Priority Issues

1. Duplicate Plaid script / provider initialization
   - Files: components/plaid-context/plaid-context.tsx, components/plaid-link/plaid-link.tsx, app/(root)/layout.tsx (layout provides PlaidProvider in some app trees)
   - Symptom: Browser console warning reported during earlier E2E runs: "The Plaid link-initialize.js script was embedded more than once." This can cause flakiness in Playwright tests and in the browser (multiple script instances fighting over global state).
   - Reproducibility: Observed intermittently in Playwright runs (earlier full validation output). Reproduces when both a provider (layout) and local initialization are present on the same page.
   - Suggested Fix: Prefer a single PlaidProvider at a high-level layout. Update Plaid consumer components to use a safe provider-aware hook and avoid locally injecting the script if a provider exists. Add defensive try/catch around link-token creation to avoid uncaught exceptions bubbling to the dev server during E2E.

2. Transient server aborts (ECONNRESET / aborted)
   - Files: actions/plaid.actions.ts, actions/dwolla.actions.ts (areas that call external services). [FLAG: some historical references to `lib/actions/*` may remain in older docs]
   - Symptom: Server-side "Error: aborted" / ECONNRESET traces observed in earlier Playwright runs (likely from external network calls failing or dev-server interruptions).
   - Reproducibility: Intermittent (network-dependent). Can surface during full E2E runs.
   - Suggested Fix: Add defensive error handling in server actions that call external services. Where possible, mock external calls in E2E test setup or add retry/backoff for transient failures. Do not log secrets or token payloads.

3. Flaky Playwright tests (my-wallets spec)
   - Files: tests/e2e/my-wallets.spec.ts, tests/e2e/helpers/\*
   - Symptom: Test suite failed in a full earlier Playwright run but passed when executed in isolation — classic flakiness due to shared state, timing, or order dependency.
   - Suggested Fix: Re-run failing suites in isolation to capture artifacts (screenshots/traces). Harden tests by avoiding reliance on global state, clearing cookies/storage between tests, and increasing relevant timeouts. Ensure external calls are mocked/stubbed when they affect determinism.

## Medium Priority Issues

4. Zod schema lint/validation gaps (describe / messages)
   - Files: various Zod schemas across the codebase (search for z.object in actions and components)
   - Symptom: ESLint zod rules require `.describe(...)` and validator messages. Missing metadata will cause lint failures when linting is executed.
   - Suggested Fix: Add `.describe("...")` to Zod schema fields and include messages on validators (z.string().min(1, "message")). Apply in small, focused commits per schema.

5. Potential N+1 DB queries in DAL
   - Files: dal/**, database/**
   - Symptom: Patterns that query inside loops can create N+1 query problems and performance regressions.
   - Suggested Fix: Audit DAL methods and replace client-side loops with JOINs or batched queries. Add unit tests for any refactor.

## Low Priority / Notes

- Bcrypt usage: Ensure bcrypt (not bcryptjs) is used and hashing cost is consistent (cost 12). Files: lib/auth-options.ts, actions/register.ts
- Env access: Confirm no direct process.env reads beyond proxy.ts; prefer app-config.ts or lib/env.ts.
- Documentation: Add the discovery plan and outputs to docs for maintainers to reproduce the validation locally.

## Next Steps (recommended, ordered)

1. Re-run Phase 2 validations and capture the outputs into `.opencode/outputs/init-enhanced/<timestamp>/` (format, type-check, lint:strict, test:browser, test:ui). Ensure port 3000 is free before Playwright.
2. If Playwright fails, re-run failing suites in isolation to collect screenshots/traces and attach to the issue entries.
3. Apply small, focused fixes in separate commits:
   - Fix provider / Plaid duplication (already prepared in working tree — review and commit separately).
   - Add defensive error handling to server actions that call external services.
   - Fix Zod `.describe()` and validator messages incrementally.
4. Re-run validations and produce a final summary report.

## Appendix — files inspected during discovery

- components/plaid-context/plaid-context.tsx (provider + safe hook)
- components/plaid-link/plaid-link.tsx (consumer, provider-aware)
- actions/plaid.actions.ts, lib/plaid.ts
- actions/dwolla.actions.ts
- tests/e2e/my-wallets.spec.ts and tests/e2e/helpers

If you want, I can now: (A) commit the prepared code fixes (they are present in the working tree but not staged) in small focused commits, or (B) keep this checkpoint docs-only and stop (no code commits). Choose which.
