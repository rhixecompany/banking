# Section 23 — Release & Deployment

- Build with `npm run build` and run `npm run start` to verify production behavior.
- Ensure required env vars exist (NEXTAUTH_SECRET, ENCRYPTION_KEY) before starting.

Example run:

```bash
npm run build
npm run start
```
