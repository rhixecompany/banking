# Section 1 — Purpose

This fragment summarizes the purpose and scope of agentic guidance in the repository. It is intended as a short, standalone section that can be stitched into larger documentation.

- Keep instructions minimal and actionable.
- Prefer executable sources of truth (package.json, eslint.config.mts, app-config.ts) over prose.

Representative snippet (shell):

```bash
# Start the dev server
npm run dev
```
