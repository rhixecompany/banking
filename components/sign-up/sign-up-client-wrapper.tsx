"use client";

/**
 * Client wrapper for the sign-up page.
 * AuthForm is already a client component — this file is a placeholder for any
 * additional client-side sign-up logic (e.g., OAuth button handlers, analytics events).
 *
 * @module sign-up-client-wrapper
 */

// AuthForm handles all client-side form state internally.
// Add future client-side sign-up enhancements here.
export {};
