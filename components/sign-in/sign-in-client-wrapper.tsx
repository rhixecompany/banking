"use client";

/**
 * Client wrapper for the sign-in page.
 * AuthForm is already a client component — this file is a placeholder for any
 * additional client-side sign-in logic (e.g., OAuth button handlers, analytics events).
 *
 * @module sign-in-client-wrapper
 */

// AuthForm handles all client-side form state internally.
// Add future client-side sign-in enhancements here.
export {};
